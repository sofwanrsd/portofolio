# Tidak ada environment variable yang dibutuhkan saat ini.
# Konten bersumber dari src/data/seed.json (sumber tunggal kebenaran) dan
# proyek dari GitHub (endpoint publik).
#
# Saat backend MongoDB + login kustom ditambahkan nanti, salin berkas ini
# menjadi `.env` lalu isi variabel yang relevan (mis. PUBLIC_API_URL).
